<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Stroke di kanan -->
    <item android:right="1dp">
        <shape android:shape="rectangle">
            <stroke android:color="@color/yellow_300" android:width="0.7dp" />
        </shape>
    </item>

    <!-- Stroke di kiri -->
    <item android:left="1dp">
        <shape android:shape="rectangle">
            <stroke android:color="@color/yellow_300" android:width="0.7dp" />
        </shape>
    </item>

    <!-- Stroke di bawah -->
    <item android:bottom="0dp">
        <shape android:shape="rectangle">
            <stroke android:color="@color/yellow_300" android:width="0dp" />
        </shape>
    </item>
</layer-list>
